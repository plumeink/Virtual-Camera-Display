{
  "name": "virtual-camera-assistant",
  "version": "0.0.13",
  "description": "Borderless Electron Application for Virtual Camera Assistant",
  "main": "index.js",
  "scripts": {
    "start": "electron index.js",
    "build": "electron-builder",
    "dist": "electron-builder --publish=never",
    "dist-portable": "electron-builder --win portable --publish=never"
  },
  "keywords": [
    "electron",
    "virtual-camera",
    "camera"
  ],
  "author": "PlumeInk",
  "license": "MPL-2.0",
  "devDependencies": {
    "electron": "^31.0.0",
    "electron-builder": "^25.0.0"
  },
  "build": {
    "appId": "com.plumeink.virtual-camera-assistant",
    "productName": "VCA",
    "directories": {
      "output": "dist"
    },
    "files": [
      "index.js",
      "index.html",
      "package.json",
      "preload.js",
      "lang/**/*.json",
      "i18n.js"
    ],
    "win": {
      "target": "portable",
      "icon": "icon.ico"
    },
    "portable": {
      "artifactName": "${productName}-${version}-${os}-${arch}.${ext}"
    }
  }
}